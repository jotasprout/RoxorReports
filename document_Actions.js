// Document Will Close

// stores locations array when closing
rememberLocationsString = JSON.stringify(locations);
this.info.rememberedLocations = rememberLocationsString;

// stores subjects array when closing
rememberSubjectsString = JSON.stringify(subjects);
this.info.rememberedSubjects = rememberSubjectsString;

// stores weather array when closing
rememberWeatherString = JSON.stringify(weather);
this.info.rememberedWeather = rememberWeatherString;

// stores actions array when closing
rememberActionsString = JSON.stringify(actions);
this.info.rememberedActions = rememberActionsString;

// stores visibility array when closing
rememberVisibilityString = JSON.stringify(visibility);
this.info.rememberedVisibility = rememberVisibilityString;

// stores equipment array when closing
rememberEquipmentString = JSON.stringify(equipment);
this.info.rememberedEquipment = rememberEquipmentString;

// stores investigator array when closing
rememberInvestigatorsString = JSON.stringify(investigator);
this.info.rememberedInvestigators = rememberInvestigatorsString;



// stores locationMenus array when closing
rememberLocMenusString = JSON.stringify(locationMenus);
this.info.rememberedLocMenus = rememberLocMenusString;

// stores subjectMenus array when closing
rememberSubMenusString = JSON.stringify(subjectMenus);
this.info.rememberedSubMenus = rememberSubMenusString;

// stores conditionMenus array when closing
rememberConMenusString = JSON.stringify(conditionMenus);
this.info.rememberedConMenus = rememberConMenusString;

// stores actionMenus array when closing
rememberActMenusString = JSON.stringify(actionMenus);
this.info.rememberedActMenus = rememberActMenusString;

// stores visibilityMenus array when closing
rememberVisMenusString = JSON.stringify(visibilityMenus);
this.info.rememberedVisMenus = rememberVisMenusString;

// stores equipmentMenus array when closing
rememberEquipMenusString = JSON.stringify(equipmentMenus);
this.info.rememberedEquipMenus = rememberEquipMenusString;

// stores investigatorMenus array when closing
rememberInvestMenusString = JSON.stringify(investigatorMenus);
this.info.rememberedInvestMenus = rememberInvestMenusString;